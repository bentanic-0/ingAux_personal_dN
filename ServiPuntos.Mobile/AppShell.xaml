<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ServiPuntos.Mobile.Views"
    x:Class="ServiPuntos.Mobile.AppShell">

    <TabBar>
        <!-- Pestaña Tenants -->
        <Tab Title="Tenants"
                Icon="tenant_icon.svg"
                Route="tenants">
            <ShellContent
                Title="Tenants"
                ContentTemplate="{DataTemplate views:TenantsPage}"
                Route="tenants"/>
        </Tab>
        <!-- Aquí podrías añadir más tabs para puntos, login, VEAI, etc. -->
    </TabBar>
</Shell>
